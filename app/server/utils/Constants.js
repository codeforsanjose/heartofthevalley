const CACHE_KEY_FEATURES = 'features'
const DATA_FILE_NAME = 'heartofvalley-data.csv'
const DEFAULT_FEATURE_IMAGE = '/images/default_featue_image.png'

module.exports = {
  CACHE_KEY_FEATURES,
  DATA_FILE_NAME,
  DEFAULT_FEATURE_IMAGE,
}
